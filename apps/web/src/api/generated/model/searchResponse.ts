/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * DSM — API de administración del catálogo (US-001)
 * Panel del dueño: categorías y productos, máquina de estado y RBAC admin. Errores en envelope RFC 7807 (`application/problem+json`, `type` `dsm:catalog/*`).
 * OpenAPI spec version: 1.0.0
 */
import type { SearchResponseConfidence } from './searchResponseConfidence';
import type { SearchResponseFallback } from './searchResponseFallback';
import type { SearchResult } from './searchResult';

export interface SearchResponse {
  results: SearchResult[];
  /** high = el top supera SEARCH_MIN_SCORE. low = hay candidatos por debajo del umbral (el frontend los presenta CON RESERVA, no los esconde). none = sin señal. La distinción low/none es lo que permite ser honesto en vez de elegir entre mentir o esconder resultados que quizás sirven. */
  confidence: SearchResponseConfidence;
  /**
     * «Interpretación visible» del design-system §7.12, DERIVADA de las categorías de los productos que matchearon — no generada por un LLM (OQ-BE-3). Dice dónde miró el sistema; no finge entender. Como el texto del cliente nunca llega a un modelo generativo, AC-8 es estructural.
     * @nullable
     */
  interpreted_as: string | null;
  /** true cuando la respuesta salió por full-text porque el proveedor de IA falló o agotó su timeout (AC-4). Viaja en el CUERPO y con 200: el frontend tiene que poder avisar «esto es el plan B» sin tratarlo como falla. Con el free tier es un estado común, no excepcional. */
  degraded: boolean;
  /**
     * Salida ofrecida cuando la respuesta no convence (confidence low o none). null sólo con confidence high.
     * @nullable
     */
  fallback: SearchResponseFallback;
}
