/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * DSM — API de administración del catálogo (US-001)
 * Panel del dueño: categorías y productos, máquina de estado y RBAC admin. Errores en envelope RFC 7807 (`application/problem+json`, `type` `dsm:catalog/*`).
 * OpenAPI spec version: 1.0.0
 */

/**
 * pending + abandoned + hechos = total. Se separan porque se resuelven distinto: `pending` se resuelve esperando la próxima corrida y `abandoned` NO se resuelve solo — necesita un POST /runs con force: true.
 */
export interface EnrichmentCoverage {
  /** Productos del catálogo, en cualquier estado. */
  total: number;
  /** Con description_enriched escrita (por la IA o curada por el dueño). */
  enriched: number;
  /** Con fila en product_embeddings. ESTE es el que decide si la búsqueda semántica funciona. */
  embedded: number;
  /** Elegibles para la próxima corrida. */
  pending: number;
  /** Agotaron ENRICHMENT_MAX_ATTEMPTS. Siguen visibles en la tienda con su description_raw (AC-5): el catálogo pierde calidad de búsqueda, no productos. */
  abandoned: number;
  /**
     * embedded / total, o 0 con catálogo vacío. El KPI de AC-3 (>= 0,9) se mide acá, sobre el catálogo real y después de una corrida: no es una propiedad del código.
     * @minimum 0
     * @maximum 1
     */
  coverage_ratio: number;
}
