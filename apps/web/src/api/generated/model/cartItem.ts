/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * DSM — API de administración del catálogo (US-001)
 * Panel del dueño: categorías y productos, máquina de estado y RBAC admin. Errores en envelope RFC 7807 (`application/problem+json`, `type` `dsm:catalog/*`).
 * OpenAPI spec version: 1.0.0
 */
import type { CartItemAvailability } from './cartItemAvailability';
import type { CartItemCurrency } from './cartItemCurrency';

/**
 * Línea del carrito (US-007). Sin product_id, cart_id, status ni stock crudo: el identificador público es el slug y el inventario se comunica como availability + max_quantity (OQ-BE-2).
 */
export interface CartItem {
  slug: string;
  name: string;
  /** @nullable */
  image_url: string | null;
  /** @minimum 1 */
  quantity: number;
  /** Precio VIGENTE en centavos ARS (AC-9), no la instantánea guardada. */
  unit_price_ars_cents: number;
  currency: CartItemCurrency;
  /** unit_price_ars_cents × quantity. Se calcula también en las líneas bloqueadas, aunque no entren en el total. */
  subtotal_ars_cents: number;
  /** available: publicado con stock ≥ cantidad. insufficient_stock: publicado con stock < cantidad (AC-6). unavailable: draft o archived (AC-6). La línea nunca se borra sola. */
  availability: CartItemAvailability;
  /** Presente sólo cuando la línea pide más de lo que hay (OQ-BE-2). */
  available_quantity?: number;
  /** min(stock, CART_MAX_QTY_PER_LINE) — techo del stepper. */
  max_quantity: number;
  /** true si el precio vigente difiere de la instantánea del último toque: el cambio se muestra, no se aplica en silencio. */
  price_changed: boolean;
  /** Presente sólo si price_changed es true. */
  previous_unit_price_ars_cents?: number;
}
