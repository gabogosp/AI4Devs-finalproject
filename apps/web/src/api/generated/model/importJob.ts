/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * DSM — API de administración del catálogo (US-001)
 * Panel del dueño: categorías y productos, máquina de estado y RBAC admin. Errores en envelope RFC 7807 (`application/problem+json`, `type` `dsm:catalog/*`).
 * OpenAPI spec version: 1.0.0
 */
import type { ImportJobPagination } from './importJobPagination';
import type { ImportJobSourceFormat } from './importJobSourceFormat';
import type { ImportJobStatus } from './importJobStatus';
import type { ImportRowError } from './importRowError';

/**
 * Estado del trabajo. NO incluye `idempotency_key` (credencial de reintento del cliente) ni `heartbeat_at` (plumbing del reaper) ni `created_by_subject`.
 */
export interface ImportJob {
  id: string;
  status: ImportJobStatus;
  /** Metadata del archivo subido; nunca se usó como ruta. */
  filename: string;
  source_format: ImportJobSourceFormat;
  /**
     * null hasta que termina la lectura del archivo.
     * @nullable
     */
  total_rows: number | null;
  processed_rows: number;
  created_count: number;
  updated_count: number;
  /** Total REAL de filas rechazadas, aun con el reporte recortado. */
  failed_count: number;
  categories_created_count: number;
  /**
     * Fallo global: `interrupted` (el proceso se reinició y el reaper lo cerró), `missing-columns`, `row-limit-exceeded`, `unsupported-format`, `invalid-encoding` o `internal`. null si los fallos fueron por fila.
     * @nullable
     */
  error_code: string | null;
  /** @nullable */
  error_message: string | null;
  /** true si se dejaron de persistir filas al llegar a IMPORT_MAX_REPORT_ROWS. */
  report_truncated: boolean;
  /** @nullable */
  started_at: string | null;
  /** @nullable */
  finished_at: string | null;
  created_at: string;
  errors: ImportRowError[];
  /** `total` son las filas rechazadas PERSISTIDAS (≤ failed_count). */
  pagination: ImportJobPagination;
}
