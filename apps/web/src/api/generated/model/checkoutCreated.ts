/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * DSM — API de administración del catálogo (US-001)
 * Panel del dueño: categorías y productos, máquina de estado y RBAC admin. Errores en envelope RFC 7807 (`application/problem+json`, `type` `dsm:catalog/*`).
 * OpenAPI spec version: 1.0.0
 */
import type { CheckoutCreatedStatus } from './checkoutCreatedStatus';

export interface CheckoutCreated {
  /**
     * Token opaco de 256 bits, hex. Se entrega UNA SOLA VEZ; en base vive su SHA-256 (orders.access_token_hash). Es la entrada de POST /v1/payments (US-009) — mismo pattern que ese contrato.
     * @pattern ^[0-9a-f]{64}$
     */
  order_token: string;
  /**
     * Número legible (SEQUENCE START WITH 1000, OQ-BE-4). No autoriza nada — la autorización es order_token. El order_id UUID no se expone.
     * @minimum 1000
     */
  order_number: number;
  /** Primer estado de la FSM (E2E §12). Inerte hasta US-010. */
  status: CheckoutCreatedStatus;
  /** Centavos ARS (api-standards §5.5), IVA incluido. Suma exacta de los subtotales del snapshot, no del carrito. */
  total_ars_cents: number;
  /** Líneas distintas de la orden. */
  items_count: number;
}
