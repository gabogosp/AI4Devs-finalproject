# Materialized index of User Story status.
# Written on US creation + status transitions (read-only for consumers).
# Source of truth: docs/user-stories/US-*.md frontmatter + §6 Dependencias.
# Conforma a us-status.schema.yaml: id corto (US-NNN) + slug; prd-capacity; blocked_by (DAG).
- id: US-001
  slug: admin-catalogo-productos
  status: Done
  priority: High
  prd-capacity: 1
  disciplines: [BE, FE, QA, INFRA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-07-11        # first plan invocation (infrastructure-developer)
  done-at: 2026-08-09               # 4/4 changes de disciplina archivados
  blocked_by: []
- id: US-002
  slug: storefront-navegacion-categorias
  status: Done
  priority: High
  prd-capacity: 1
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-16
  in-review-at: 2026-08-30T12:30:00Z # PR #21 (qa) abierto; BE y FE siguen sin rama/PR propios (branch: null) — US-level se queda In Progress
  # NOTA (2026-09-05, /archive-change): QA archivó (PR #21 mergeado 2026-08-30). `status`
  # NO avanza — BE y FE siguen sin plan/rama/PR (branch: null en ambos), no sólo sin
  # archivar.
  #
  # NOTA (2026-09-05, /archive-change US-002-storefront-navegacion-categorias-{backend,
  # frontend-web}, barrido de changes pendientes — PR #3 mergeado 2026-08-29): la nota
  # anterior quedó vieja — BE (18/18 tasks) y FE (33/33 tasks) SÍ estaban mergeados
  # (mismo PR #3 bundled que US-004/005/007/014), sólo sin branch propio (modelo de
  # rama de integración previo a la política de una rama por change). Ambas
  # disciplinas archivadas ahora. Con QA ya archivado también, las 3/3 disciplinas
  # están resueltas → `status` avanza a `Done`.
  done-at: 2026-09-05
  blocked_by: [US-001]
- id: US-003
  slug: ficha-producto-pdp
  status: Done
  priority: High
  prd-capacity: 1
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-15
  done-at: 2026-08-20               # 3/3 changes de disciplina archivados
  blocked_by: [US-001]
- id: US-004
  slug: busqueda-semantica
  status: Done
  priority: High
  prd-capacity: 2
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-22        # primera planificación (/plan-backend-ticket)
  done-at: 2026-08-29               # BE + FE archivados (QA vive dentro del BE)
  blocked_by: [US-005]              # US-005 va 1/28: su migración está aplicada, el pipeline en vuelo
  # 2026-08-29: US-004-busqueda-semantica-frontend-web ARCHIVADA (29/29 tasks, merged vía PR #3
  # feature-entrega2-GOSP). La US NO pasa a Done: quedan 2 de 3 disciplinas sin archivar —
  # BE (US-004-busqueda-semantica-backend, status completed, ya mergeado, pendiente de
  # /archive-change) y QA (US-004-busqueda-semantica-qa, PR #10 abierto, no mergeado todavía).
- id: US-005
  slug: enriquecimiento-ia-embeddings
  status: In Progress
  priority: High
  prd-capacity: 3
  disciplines: [BE, QA, INFRA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-22        # first plan invocation (backend-node-developer)
  in-review-at: 2026-09-06   # /commit — QA (primera disciplina en llegar a in-review) PR #68.
  # INFRA sigue sin planificar — US-level se queda In Progress.
  #
  # NOTA (2026-09-05, /archive-change US-005-enriquecimiento-ia-embeddings-backend, barrido
  # de archivos pendientes — PR #3 mergeado 2026-08-29): disciplina backend archivada
  # (openspec/changes/archive/US-005-enriquecimiento-ia-embeddings-backend/), 28/28 tasks
  # AS-BUILT. Crea la capacidad nueva `openspec/specs/enriquecimiento-ia/` (CAP-3, puente
  # entre catálogo y búsqueda semántica) — README + requirements + decisions + contratos
  # vivos (2 endpoints admin: GET /admin/enrichment/status, POST /admin/enrichment/runs).
  # `status` de la US NO avanza a `Done`: INFRA (tercera disciplina declarada) nunca se
  # planificó — no hay `openspec/changes/US-005-*-infrastructure/`.
  # NOTA (2026-09-06, /archive-change US-005-enriquecimiento-ia-embeddings-qa, PR #68
  # mergeado a9e0a18): disciplina QA archivada
  # (openspec/changes/archive/US-005-enriquecimiento-ia-embeddings-qa/). BDD 16/16,
  # contract 12/12, k6 dentro de NFR, más el fix de higiene de entorno compartido
  # (QA-005-F1: `ENRICHMENT_ENABLED=false` default en `qa/scripts/api-up.sh`). No se repiten
  # `QA-004-REL-*`/`QA-004-PERF-3`, ya derivadas al qa-plan de US-004. Con BE + QA
  # archivados, `status` de la US SIGUE sin avanzar a `Done`: INFRA (tercera disciplina
  # declarada) nunca se planificó — no hay `openspec/changes/US-005-*-infrastructure/`. Se
  # queda en
  # `In Progress` (INFRA pendiente, nunca planificada). `blocked_by` sin tocar (propiedad de
  # otro agente).
  blocked_by: [US-001, US-006]
- id: US-006
  slug: import-masivo-inventario
  status: Done
  priority: Medium
  prd-capacity: 7
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-19        # first plan invocation (backend-node-developer)
  in-review-at: 2026-08-29T21:25:05Z # PR #8 (qa) abierto; BE y FE ya "completed" sin PR discreto
                                      # (modelo de rama de integracion)
  done-at: 2026-08-30               # 3/3 discipline changes archivados (BE+FE via PR #3, QA via PR #8)
  blocked_by: [US-001]
- id: US-007
  slug: carrito-compra
  # Las TRES disciplinas con sus tasks al 100% el 2026-08-23 (backend 37/37,
  # frontend-web 36/37 — 1 gate humano de consola en dev sin cerrar, ver
  # openspec/specs/carrito/requirements.md D-6 —, qa 22/22), mergeadas a main vía PR #3
  # (feature-entrega2-GOSP, 2026-08-29). Las TRES disciplinas ya archivaron
  # (/archive-change): backend 2026-08-29, frontend-web 2026-08-30, qa 2026-08-30.
  # NO pasa a Done: el DoD de la US (gate QA → Done) exige regresión verde en staging,
  # AC manuales verificados por QA y firma del PO — ninguno tildado todavía; archivar
  # las tres disciplinas cierra el código, no el gate humano.
  status: QA
  priority: High
  prd-capacity: 4
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-20        # primera planificación (/plan-backend-ticket)
  blocked_by: [US-003]
- id: US-008
  slug: checkout-guest
  status: Done   # /develop-qa US-008 (PR #64, mergeado 2026-09-06) — 3/3 disciplinas
                 # resueltas: BE (#11, archivado), FE (#23, archivado), QA (#64, Mode A
                 # inline en el backend ya archivado, sin change `-qa` propio que archivar).
                 # done-at abajo.
  priority: High
  prd-capacity: 4
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-22        # primera planificación (/plan-backend-ticket)
  in-review-at: 2026-08-30          # FE (PR #23); BE ya estaba en review (PR #11) sin este campo
                                     # estampado — QA sin planificar, status US se queda en Progress
  #
  # NOTA (2026-09-05, /archive-change US-008-checkout-guest-frontend-web, barrido de
  # archivos pendientes — PR #23 mergeado 2026-08-30): disciplina frontend-web archivada
  # (openspec/changes/archive/US-008-checkout-guest-frontend-web/), 39/39 tasks. Extiende
  # openspec/specs/checkout/ (CAP-10, ya existente desde el backend) con la sección "Desde
  # US-008 frontend-web" en requirements.md + decisions.md (resolver de validación sobre
  # schema generado, order_token en sessionStorage, confirmación in-place). BE también ya
  # está archivado. `status` de la US NO avanza a `Done` ni a `QA`: la disciplina QA nunca
  # se planificó (no existe openspec/changes/US-008-*-qa/ en ningún estado). Se queda en
  # `In Progress`. `blocked_by` sin tocar (propiedad de otro agente).
  #
  # NOTA (2026-09-05, /develop-qa US-008): la nota de arriba estaba desactualizada — el
  # `qa-plan.md` sí existía (Mode A, inline en `US-008-checkout-guest-backend/`, escrito
  # 2026-08-23, regenerado 2026-08-30), sólo nunca se había ejecutado. Ejecutado ahora
  # contra el backend+frontend-web ya mergeados: 14/14 escenarios de aceptación, 5/5 casos
  # de contract testing, k6 p95 ≈4ms (vs. umbral 500ms), 2/2 casos del E2E Playwright
  # cross-stack (incluida SC-008-X3, enlaces de consentimiento reales) — todo verde, sin
  # defectos en la implementación. Con esto BE + FE + QA de US-008 están completos (QA no
  # requiere archive-change separado, Mode A ya vive en el directorio archivado). `status`
  # queda en `In Progress` — la transición a `Done` la aplica quien mergee este PR.
  # `blocked_by` sin tocar (propiedad de otro agente).
  #
  # NOTA (2026-09-06, PR #64 mergeado): las 3 disciplinas están resueltas — BE y FE
  # archivados, QA ejecutada y verde (Mode A, sin change `-qa` propio que archivar, mismo
  # caso que US-021 QA). `status` avanza a `Done`. `blocked_by` sin tocar.
  done-at: 2026-09-06
  blocked_by: [US-007]
- id: US-009
  slug: pago-mercadopago
  status: Deferred        # 2026-09-06: decisión de producto del PO — MercadoPago real
  # (pago online con tarjeta) queda FUERA DE ALCANCE del MVP. US-023-pago-manual-offline
  # (ya Done) es el único medio de pago del MVP y cubre transaccionar. No es deuda
  # técnica: es una decisión de alcance explícita, gate 3 del cierre de dev de US-022.
  # PaymentConfirmationPort (extraído desde el diseño de US-023) sigue dejando a
  # MercadoPago como un adaptador enchufable si el negocio decide agregarlo post-MVP.
  # deferred-reason abajo documenta el trigger de reactivación.
  priority: High
  prd-capacity: 4
  disciplines: [BE, QA, INFRA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-22        # primera planificación (/plan-backend-ticket)
  deferred-at: 2026-09-06
  deferred-reason: "Decisión de producto del PO (2026-09-06): pago online real con MercadoPago no es necesario para el MVP — el pago manual/offline de US-023 (Done) ya permite transaccionar. Trigger de reactivación: el negocio decide ofrecer pago con tarjeta online post-MVP Y se consiguen credenciales reales de MercadoPago (bloqueo original, US-008 sin planificar)."
  blocked_by: [US-008]              # histórico — US-008 seguía sin planificar cuando se bloqueó; ya no aplica en Deferred (excluida de active-work/build-order)
- id: US-010
  slug: orden-webhook-stock
  status: Done   # /archive-change 2026-09-06: QA archivada (PR #57 mergeado ed9512a). Con
  # BE ya archivado desde 2026-09-05 (dea4f25), las 2/2 disciplinas declaradas
  # (`disciplines: [BE, QA]`) están resueltas — la US avanza a Done.
  done-at: 2026-09-06
  priority: High
  prd-capacity: 6
  disciplines: [BE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-22        # primera planificación (/plan-backend-ticket)
  in-review-at: 2026-09-05   # /commit — backend (primera disciplina) llega a in-review, PR #49, 42/42 tasks. QA todavía no arrancó — US-level se queda In Progress.
  #
  # NOTA (2026-09-05, /archive-change US-010-orden-webhook-stock-backend, PR #49 mergeado
  # dea4f25): disciplina backend archivada (openspec/changes/archive/US-010-orden-webhook-stock-backend/).
  # Extiende la capacidad `pagos` (CAP-4, ya existente desde US-023) con "Desde US-010
  # backend" en requirements.md/decisions.md/README.md/contracts — webhook de MercadoPago,
  # medio simulado «DSM», MercadoPagoClient, 3 jobs admin. Resuelve D-2/D-3 de
  # `pagos/requirements.md` (antes diferidos). Reconciliación con la capacidad `ordenes`
  # (CAP-5, US-012) confirmada sin conflicto — US-010 no la toca, vive del lado de `pagos`.
  # `status` de la US NO avanza a `Done` ni a `QA`: la disciplina QA nunca se planificó (no
  # existe `openspec/changes/US-010-*-qa/` en ningún estado, pese a `disciplines: [BE, QA]`).
  # Se queda en `In Progress`. `blocked_by` sin tocar (propiedad de otro agente).
  #
  # NOTA (2026-09-06, /archive-change US-010-orden-webhook-stock-qa, PR #57 mergeado
  # ed9512a): disciplina QA archivada (openspec/changes/archive/US-010-orden-webhook-stock-qa/).
  # Suite L1/L3 sobre la capacidad `pagos`: 15/15 BDD, 13/13 contract, k6 dentro de budget
  # (p95 4-15ms vs 200ms), 2/2 E2E Playwright, 3 charters escritos. 2 hallazgos abiertos sin
  # dueño bloqueante: AC-3 real bloqueado sin cuenta sandbox de MercadoPago (QA-010-F1,
  # owner PO/Infra) y `K6_VUS` env var reservada que pisa scripts k6 en silencio (QA-010-F2,
  # owner QA, afecta también scripts ya archivados de US-023). Con esto, 2/2 disciplinas de
  # `disciplines: [BE, QA]` están resueltas (ambas archivadas) → `status` avanza a `Done`.
  # REGENERADO 2026-09-05 (/plan-backend-ticket US-010 --regenerate): desacoplado de US-009.
  # US-008 y payments (US-023) ya existen y son lo único que este change necesita; construye
  # su propio MercadoPagoClient de alcance mínimo (getPayment/searchByExternalReference/
  # refund) en vez de esperar a que US-009 lo entregue. US-009 sigue Blocked (sin
  # credenciales) y ya no es un pre-requisito de este change — ver
  # openspec-changes.yaml (US-010-orden-webhook-stock-backend) y design.md §D6 para el
  # detalle de qué se puede construir/verificar hoy vs qué necesita la cuenta real de MP.
  blocked_by: []
- id: US-011
  slug: notificaciones-email
  status: Done   # /archive-change US-011-notificaciones-email-backend 2026-09-06 — Mode A:
  # BE (PR #82) + QA (PR #88, inline, sin change -qa propio) ambos archivados en el mismo
  # change. 2/2 disciplinas declaradas ([BE, QA], sin FE) resueltas → Done. AC-4/AC-5
  # quedan `blocked` a nivel aceptación por falta de cuenta Resend real — deuda de fase de
  # producción (secret/DNS a provisionar), no bloquea el cierre de la US.
  priority: High
  prd-capacity: 6
  disciplines: [BE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-09-06        # primera planificación (/plan-backend-ticket, backend-node-developer)
  done-at: 2026-09-06
  blocked_by: [US-010, US-012]      # ambas ARCHIVADAS (US-010 2026-09-05, US-012 2026-09-05)
  # — ya no bloquean, sin tocar el campo per index-conventions.md (mismo criterio que la
  # nota previa de este archivo).
- id: US-012
  slug: panel-ordenes-dueno
  status: Done   # 3/3 disciplinas archivadas (BE PR #35, FE PR #32, QA PR #33). Cerrada por el coordinador tras el archive sweep 2026-09-05.
  priority: High
  prd-capacity: 5
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-30        # primera planificación (/plan-frontend-web-ticket, disciplina FE)
  in-review-at: 2026-08-30   # /commit — backend (primera disciplina) llega a in-review, 22/22 tasks
  done-at: 2026-09-05        # 3/3 disciplinas archivadas (BE+FE+QA); flip de status hecho en el archive sweep
  # NOTA (2026-09-05, /archive-change US-012-panel-ordenes-dueno-qa): la disciplina QA
  # archivó (PR #30 mergeado, openspec/changes/archive/US-012-panel-ordenes-dueno-qa/).
  # `status` NO avanza a `Done` — backend y frontend-web siguen SIN archivar
  # (openspec/changes/US-012-panel-ordenes-dueno-{backend,frontend-web}/, todavía en
  # `changes/`, no en `changes/archive/`), aunque sus PRs (#22, #31) ya están
  # mergeados — sus entradas en openspec-changes.yaml quedaron en `in-review`, stale
  # respecto al merge real. No se tocan esas dos entradas ni sus directorios desde
  # este comando (fuera del alcance pedido: sólo QA); parece haber otra sesión
  # trabajando ese archive en paralelo (worktree `archive-US-012-frontend-web` visto
  # en `git worktree list`).
  # NOTA (2026-09-05, /commit US-012-panel-ordenes-dueno-qa): las 3 disciplinas ya
  # alcanzaron PR abierta — BE+FE mergearon vía PR #22 (confirmado: commit 1a90fe7 en
  # `main`, "feat: US-012 panel de órdenes del dueño — backend + frontend (QA pendiente)
  # (#22)") y QA acaba de abrir PR #30 (24/24 tasks: seed real + aceptación 13/13 + E2E
  # 5/5 + a11y 5/5 + carga 2/2 + charters 2/2). `status` avanza de `In Progress` a
  # `In Review` recién ahora — la nota de abajo (2026-08-30) quedó vieja: en ese momento
  # sólo backend tenía código.
  # `status`/`blocked_by` sin tocar: propiedad de prd-decomposer/user-story-author
  # (index-conventions.md §84). El bloqueo real sigue vigente — ver
  # openspec/changes/US-012-panel-ordenes-dueno-frontend-web/proposal.md
  # §"Dependencia cruzada": ni US-010-backend (Draft, 0 tasks) ni
  # US-012-backend (sin planificar) existen todavía.
  # NOTA (2026-08-30, /plan-backend-ticket): US-012-panel-ordenes-dueno-backend ya está
  # planificado (openspec/changes/US-012-panel-ordenes-dueno-backend/, draft, 0 tasks
  # cerradas). No se toca `in-progress-at` (ya lo puso la sesión de FE, primera disciplina
  # en planificarse) ni `blocked_by` (sigue siendo propiedad de otro agente) — el bloqueo
  # real de la US completa sigue siendo US-010, ahora con las tres disciplinas de US-012
  # (FE, BE) planificadas y la de QA todavía pendiente.
  #
  # NOTA (2026-08-30, /plan-backend-ticket US-012 --regenerate): el backend se replanificó
  # de cero (US-009 → Blocked, US-010 pospuesta sin fecha; ver US-023-pago-manual-offline
  # como camino interino). El regenerate de US-012-backend YA NO depende de US-010 en
  # código — `orders` (migrada por US-008) alcanza para las 4 transiciones de fulfillment
  # que este panel gestiona; su tasks.md no tiene gate en rojo. Sigue sin tocarse
  # `blocked_by` (propiedad de otro agente, mismo criterio que la nota de arriba) — pero
  # el bloqueo real declarado en ese campo (US-010) ya no aplica al backend de esta US;
  # sí puede seguir aplicando al sibling FE (su propio proposal.md lo declara — no editado
  # por este agente). Quien posea `blocked_by` debería revisar si vale la pena bajarlo a
  # `[]` o mantenerlo por el lado FE.
  #
  # NOTA (2026-08-30, /plan-qa): US-012-panel-ordenes-dueno-qa ya está planificado
  # (openspec/changes/US-012-panel-ordenes-dueno-qa/, draft, 0 tasks cerradas) — las tres
  # disciplinas de la US (FE, BE, QA) ya tienen plan. Ningún test case de QA es ejecutable
  # hoy: los 13 de aceptación se desbloquean con solo el backend, los 9 restantes (E2E,
  # a11y, exploratorio-FE) necesitan además el frontend. No se toca `in-progress-at` ni
  # `blocked_by` (propiedad de otro agente, mismo criterio que las notas anteriores).
  #
  # NOTA (2026-08-30, /develop-frontend-web + /commit): frontend-web pasó de 0/25 a 21/26
  # tasks (Fase 12 creció en 1 durante la ejecución — T0.3 explícito). Las 5 restantes
  # (T0.3, T12.1-T12.4) están genuinamente bloqueadas por `US-023-pago-manual-offline-backend`
  # (contrato `pending-payment` todavía no publicado) — no es un stop, es el límite real de
  # lo ejecutable hoy. `status` de la US se queda en `In Progress` (backend en-review, FE
  # checkpoint sustancial pero no completo, QA todavía sin ejecutar).
  #
  # NOTA (2026-09-02, merge de origin/main sobre esta rama): `main` traía un commit directo
  # (`chore(index): US-012 -> In Progress`, 2026-08-31) que pisaba este bloque con una versión
  # resumida y `in-progress-at: 2026-08-31`. Se conserva el historial de esta rama —es un
  # superset informativo— y la fecha original 2026-08-30 (anterior, y la que corresponde a la
  # PRIMERA transición a `In Progress`). PR #27 (US-023 backend, `pending-payment`) ya mergeó a
  # `main` en este mismo merge — el bloqueo de T0.3/T12.1-T12.4 de la nota anterior debería
  # revisarse en la próxima corrida de /develop-frontend-web.
  #
  # NOTA (2026-09-05, /develop-frontend-web, worktree aislado
  # `.claude/worktrees/US-012-pendientes-pago-frontend-web`): T0.3 desbloqueada
  # (`US-023-pago-manual-offline-backend` mergeó PR #27) — T12.1-T12.4 (PendingPaymentsPanel)
  # cerradas. frontend-web queda en 26/26 tasks, pendiente sólo `/commit`. `status` de la US
  # se queda en `In Progress` (backend en-review, QA todavía sin ejecutar) — no se toca
  # `blocked_by` (propiedad de otro agente).
  #
  # NOTA (2026-09-05, /commit): frontend-web abrió PR #31 contra `main`
  # (`feat/US-012-pendientes-pago-frontend-web`) — segunda disciplina en llegar a in-review
  # (backend ya lo estaba desde 2026-08-30). No se toca `in-review-at` (ya lo puso backend,
  # primera disciplina) ni el `status` de la US (se queda en `In Progress` — QA sigue en
  # `draft`, 0/24 tasks, ninguna PR abierta todavía).
  #
  # NOTA (2026-09-05, barrido de archives): backend (este change, 29/29 tasks) y
  # frontend-web (PR #32) YA archivados a openspec/changes/archive/. El backend creó la
  # capacidad nueva `openspec/specs/ordenes/` (CAP-5, panel de fulfillment) con su
  # contrato vivo. Falta archivar la disciplina qa (PR #33) para que la US pase a Done —
  # hasta entonces `status` se queda en `In Progress`. `blocked_by` sin tocar.
  blocked_by: [US-010]
- id: US-013
  slug: cancelacion-reembolso
  status: Done
  priority: Medium
  prd-capacity: 11
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-09-06        # first plan invocation (backend-node-developer)
  in-review-at: 2026-09-06          # backend (primera disciplina) — PR #66 abierto, 20/20 tasks + suite completa 225/225 verde
  blocked_by: []
  # US-010 y US-012 (bloqueantes declaradas) ya están archivadas — MercadoPagoClient.refund,
  # refund_pending/retry-refunds, NotificationPort, order_status_history y el CHECK de 6
  # estados de orders.status ya existen. Plan de backend en
  # openspec/changes/US-013-cancelacion-reembolso-backend/ — sin migración de Prisma
  # (design.md §D2), vive en apps/api/src/payments/ (no en orders/) para preservar la
  # dirección acíclica payments → orders ya establecida (design.md §D1).
  #
  # NOTA (2026-09-06, /commit US-013-cancelacion-reembolso-backend): backend llega a
  # in-review — 20/20 tasks (12 fases), suite completa apps/api 225/225 suites/1877/1877
  # tests, lint+typecheck+spectral limpios, PR #66 abierto. `status` de la US se queda en
  # `In Progress` (FE/QA todavía sin planificar).
  #
  # NOTA (2026-09-06, /commit US-013-cancelacion-reembolso-frontend-web): backend (#66)
  # mergeado a main; FE (segunda disciplina) llega a in-review — 17/17 tasks (7 fases),
  # suite completa apps/web 153/153 archivos/968/968 tests, lint+typecheck limpios,
  # codegen fresco, PR #67 abierto. `status` de la US se queda en `In Progress` (QA
  # todavía sin planificar).
  #
  # NOTA (2026-09-06, /commit US-013-cancelacion-reembolso-qa): BE (#66) y FE (#67)
  # ya mergeados a main. QA (última disciplina) llega a in-review — 13/13 test cases
  # automatizados verdes + 2 charters documentados, PR #73 abierto. QA-013-E2E-3
  # encontró un defecto real en apps/web (mensaje de resultado de cancelar nunca se
  # veía), resuelto por separado en PR #72 (ya mergeado) y re-verificado. Las 3
  # disciplinas tienen PR — `status` de la US se queda en `In Progress` (sin
  # precedente de `In Review` a nivel US en este índice); avanza a `Done` recién
  # cuando las 3 disciplinas queden archivadas (`/archive-change`).
  #
  # NOTA (2026-09-06, /archive-change US-013-cancelacion-reembolso-backend): PR #66
  # mergeado a main (e296d3f). Archivado a openspec/changes/archive/. Extiende
  # openspec/specs/pagos/ (CAP-4) con POST /admin/orders/{id}/cancel. FE (#67) y QA
  # (#73) ya mergeadas a main pero todavía sin archivar al momento de este archive
  # (se archivan a continuación, una disciplina a la vez) — `status` de la US se
  # queda en `In Progress`.
  #
  # NOTA (2026-09-06, /archive-change US-013-cancelacion-reembolso-frontend-web): PR
  # #67 mergeado a main (62437f7). Archivado a openspec/changes/archive/. Extiende
  # openspec/specs/pagos/ con la acción "Cancelar orden" (OrderCancelAction.tsx).
  # Documenta el defecto real que encontró QA-013-E2E-3 (mensaje de éxito nunca se
  # veía) y su resolución vía PR #72 (ya mergeado, previo a este archive). Sólo falta
  # QA (#73, ya mergeada a main) para que la US pase a `Done`.
  #
  # NOTA (2026-09-06, /archive-change US-013-cancelacion-reembolso-qa): PR #73
  # mergeado a main. Archivado a openspec/changes/archive/. Última disciplina de las
  # 3 (BE #66/FE #67/QA #73, todas `archived`) — US pasa a `Done`. QA encontró y
  # verificó el fix de un defecto real (mensaje de éxito de cancelar nunca se veía,
  # PR #72) antes de este cierre.
  done-at: 2026-09-06
- id: US-014
  slug: registro-login
  status: Done
  priority: Medium
  prd-capacity: 8
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-17
  #
  # NOTA (2026-09-05, /archive-change US-014-registro-login-backend, barrido de
  # archivos pendientes — PR #3 mergeado 2026-08-29): disciplina backend archivada
  # (openspec/changes/archive/US-014-registro-login-backend/), 44/44 tasks. Crea la
  # capacidad nueva `openspec/specs/cuentas/` (CAP-6) — README + requirements +
  # decisions + contratos vivos (8 endpoints: 7 de /auth/* + /admin/auth/login
  # mudado desde catalogo). `status` de la US NO avanza a `Done` ni a `QA`: FE
  # (US-014-registro-login-frontend-web) también está mergeado pero sin archivar
  # todavía, y QA (US-014-registro-login-qa, PR #9 mergeado) tiene un `[Open]` real
  # (NFR de latencia de login sin ratificar por PO/Arquitecto) que bloquea su
  # cierre. Se queda en `In Progress`. `blocked_by` sin tocar (propiedad de otro
  # agente).
  #
  # NOTA (2026-09-05, /archive-change US-014-registro-login-frontend-web): también
  # archivada, 33/33 tasks. Extiende openspec/specs/cuentas/ con la UI de sesión —
  # rewrite same-origin (ADR-0013 nuevo), refresh single-flight cross-tab, AC-5
  # protegido en el cliente. BE + FE de US-014 ya están archivados (2/3
  # disciplinas). `status` SIGUE sin avanzar a `Done`: QA tiene el `[Open]` real
  # descripto arriba y no se puede cerrar hasta que el PO/Arquitecto ratifique el
  # NFR de latencia de login. Se queda en `In Progress`.
  #
  # NOTA (2026-09-06, /archive-change US-014-registro-login-qa): el PO ratificó el NFR
  # de login (p95 ≤ 800ms, PR #86) — OQ-QA-5 resuelto, re-verificado con k6 real
  # (p95=654,41ms). Archivado a openspec/changes/archive/. Última disciplina de las 3
  # (BE/FE/QA, todas `archived`) — US pasa a `Done`. Deuda conocida no bloqueante
  # documentada en openspec/specs/cuentas/README.md: regresión no re-corrida
  # explícitamente (cubierta de facto por CI continua) y los 2 charters manuales
  # TC-170/TC-171 pendientes de ejecución humana (fase de prueba local/visual del
  # usuario).
  done-at: 2026-09-06
  blocked_by: [US-001]
- id: US-015
  slug: historial-compras
  status: Done   # /archive-change US-015-historial-compras-frontend-web 2026-09-06 —
                 # 3/3 disciplinas archivadas (BE, FE, QA). done-at abajo.
  # NOTA histórica: primera planificación 2026-09-06 (/plan-backend-ticket, disciplina BE).
  # Alcance ampliado por decisión del PO: BE construye también el escritor de
  # `orders.customer_id` que US-008 dejó diferido a esta US (ver
  # openspec/changes/US-015-historial-compras-backend/proposal.md §Why).
  priority: Medium
  prd-capacity: 8
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-09-06   # primera planificación (/plan-backend-ticket)
  in-review-at: 2026-09-06     # backend PR #70 (mergeado) + FE PR #74 abierto (/commit) +
                                # QA PR #79 abierto (/commit) — 3/3 disciplinas en review.
  #
  # NOTA (2026-09-06, /archive-change US-015-historial-compras-qa, PR #79 mergeado
  # c9166df): disciplina QA archivada (openspec/changes/archive/US-015-historial-compras-qa/).
  # `status` de la US NO avanza a `Done` todavía: BE (PR #70/#71) y FE (PR #74) siguen
  # `in review` — mergeados a main pero sin su propio `/archive-change` corrido. Se queda
  # en `In Review`. La capacidad `historial-compras` (CAP-8) todavía no existe — se crea
  # recién cuando BE archive; ese archive debe sumar la sección "Qué verificó QA" con lo
  # que este change dejó documentado (ver nota en su `design.md` archivado).
  #
  # NOTA (2026-09-06, /archive-change US-015-historial-compras-backend, PR #70/#71
  # mergeados): disciplina BE archivada
  # (openspec/changes/archive/US-015-historial-compras-backend/). Crea
  # `openspec/specs/historial-compras/` (CAP-8, primera entrega) con la sección "Qué
  # verificó QA" que dejó pendiente la nota de arriba, e incorpora la publicación del
  # contrato del follow-up (PR #71). Suma una fila a
  # `openspec/specs/checkout/decisions.md` (OptionalCustomerGuard) sin tocar su
  # contrato público. `status` de la US sigue sin avanzar: falta archivar FE (PR #74,
  # `in review`) — 2/3 disciplinas archivadas.
  #
  # NOTA (2026-09-06, /archive-change US-015-historial-compras-frontend-web, PR #74
  # mergeado 65ced11): disciplina FE archivada
  # (openspec/changes/archive/US-015-historial-compras-frontend-web/). Extiende
  # `openspec/specs/historial-compras/` con la sección FE (rutas `/mi-cuenta/compras`,
  # decisiones D8-D11) — sin delta de contrato de API (consumo puro del contrato que BE
  # ya publicó). Con esto, las 3/3 disciplinas quedan archivadas (BE, FE, QA) — `status`
  # avanza a `Done`.
  done-at: 2026-09-06
  blocked_by: [US-014, US-010]
- id: US-016
  slug: panel-metricas
  status: Done
  priority: Medium
  prd-capacity: 9
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-09-05        # first plan invocation (backend-node-developer)
  # NOTA (2026-09-05, /plan-backend-ticket US-016): `blocked_by` de abajo quedó
  # desactualizado — US-010-orden-webhook-stock-backend archivó el 2026-09-05. El
  # plan de backend (openspec/changes/US-016-panel-metricas-backend/) no depende de
  # código de US-010: sólo lee `orders`/`order_items`, ya migradas desde US-008. No
  # se toca `blocked_by` (propiedad de otro agente per index-conventions.md).
  #
  # NOTA (2026-09-05, /commit US-016-panel-metricas-backend): backend (primera
  # disciplina) llega a in-review — 23/23 tasks + 9/9 verificación suite-level,
  # PR #55 abierto. `status` de la US se queda en `In Progress` (FE/QA todavía sin
  # planificar).
  in-review-at: 2026-09-05
  #
  # NOTA (2026-09-05, /plan-frontend-web-ticket US-016): planificado el change
  # US-016-panel-metricas-frontend-web (draft) — consume el contrato
  # admin-reports de US-016-panel-metricas-backend (PR #55, in review) sólo vía
  # openapi.yaml (cliente ya regenerado, heredado por rama). Feature nueva
  # src/features/metrics/ (3 widgets independientes) + recharts (design-system
  # §9, ya Approved) + refactor compartido (Extract Method) de la descarga CSV
  # entre imports y metrics. No se toca `status` (FE es la segunda disciplina,
  # QA sigue sin planificar) ni `blocked_by` (propiedad de otro agente).
  #
  # NOTA (2026-09-05, /commit US-016-panel-metricas-frontend-web): FE (segunda
  # disciplina) llega a in-review — 25/25 tasks + 6/6 verificación suite-level
  # (152 archivos/947 tests), PR #59 abierto (stacked sobre #55). `status` de
  # la US se queda en `In Progress` (QA todavía sin planificar).
  #
  # NOTA (2026-09-05, /plan-qa US-016): qa-plan.md agregado Modo A embebido en
  # `US-016-panel-metricas-qa/qa-plan.md` (BE y FE ya construidos, PR #55/#59
  # in review) — 12 test cases owned-by-QA (2 aceptación BDD, 1 contract, 5 E2E de
  # navegador cross-stack, 1 a11y L3, 1 k6, 2 charters), 0 bloqueados: a diferencia
  # de US-008/US-009/US-023 (otros Modo A de este repo), las cuatro capacidades de
  # las que depende el dataset cross-feature (checkout US-008, confirmación manual
  # US-023, pago simulado US-010, fulfillment US-012) ya están archivadas. El
  # escenario central (X-1/QA-016-ACC-2/QA-016-E2E-1) siembra 6 órdenes 100% por
  # API real (checkout→confirmación/simulado→PATCH de fulfillment) para probar
  # que sólo las 4 activas cuentan como venta — algo que ningún test de un solo
  # módulo puede ver, porque el backend siembra sus propios 6 estados con
  # `prisma.order.create` directo (design.md §D2/T7.3 de ese change). `blocked_by`
  # de abajo sigue desactualizado (ver nota de arriba, 2026-09-05) — no se toca,
  # propiedad de otro agente. `status`/`in-progress-at` sin tocar.
  #
  # NOTA (2026-09-06, /archive-change US-016-panel-metricas-backend): PR #55
  # mergeado a main (72afc73). Archivado a openspec/changes/archive/. Inaugura
  # openspec/specs/metricas/ (CAP-9). FE (#59) y QA (#60) mergeadas a main pero
  # todavía sin archivar al momento de este archive (se archivan a continuación,
  # una disciplina a la vez, mismo change-set) — `status` de la US se queda en
  # `In Progress` hasta que las 3 disciplinas queden `archived`.
  #
  # NOTA (2026-09-06, /archive-change US-016-panel-metricas-frontend-web): PR
  # #59 mergeado a main (ad21184). Archivado a openspec/changes/archive/.
  # Extiende openspec/specs/metricas/ con la feature `metrics/` (asimetría de
  # nombre vs `reports`/`Reports*` del backend, documentada en decisions.md D7).
  # Sólo falta QA (#60, ya mergeada a main) para que la US pase a `Done`.
  #
  # NOTA (2026-09-06, /archive-change US-016-panel-metricas-qa): PR #60
  # mergeado a main (d9672ef). Archivado a openspec/changes/archive/. Última
  # disciplina de las 3 (BE #55/FE #59/QA #60, todas `archived`) — US pasa a
  # `Done`. `blocked_by` de abajo queda intencionalmente sin tocar aunque está
  # obsoleto (US-010 archivó el 2026-09-05, ver nota de arriba) — no es
  # bloqueante para el cierre, y su limpieza es propiedad de otro agente per
  # index-conventions.md.
  done-at: 2026-09-06
  blocked_by: [US-010]
- id: US-017
  slug: paginas-legales-consentimiento
  status: In Progress
  priority: High
  prd-capacity: 10
  # BE SACADO 2026-08-23 — resolución de OQ-FE-18, opción (a): `BE-US-017` queda ABSORBIDA por
  # US-008. La US §7 le asignaba al backend «servir el contenido legal + versionado». Las dos
  # mitades desaparecieron por decisiones de diseño ya tomadas, no por recorte de alcance:
  #   · Servir el contenido: el design.md de US-017 D1 lo fija como módulo tipado en el FE y D5
  #     la versión en el código. No hay endpoint que servir. Un endpoint para dos documentos que
  #     cambian una vez por año le agregaría una dependencia de red a una página cuya razón de
  #     ser es estar disponible siempre — lo que el guard noBackendNoTracking.test.tsx prohíbe.
  #   · Versionado: se lo quedó US-008 (`LEGAL_TERMS_VERSION` + `orders.consent_terms_version` +
  #     su CHECK). La propia US lo asigna ahí en AC-4 y AC-8. El FE verifica el contrato contra
  #     esa declaración en versionContract.test.ts, en vez de construirlo.
  # Se saca del array en vez de dejarlo como task fantasma para que el flow-auditor no lea una
  # disciplina sin cobertura (opción (b) descartada, con su costo escrito en el proposal).
  disciplines: [FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-22        # first plan invocation (frontend-web-developer)
  blocked_by: [US-001]
  # QA SE MANTIENE, sin change propio: es la norma del repo (15 de 20 US con QA en `disciplines`
  # no tienen change `-qa`; US-004/005/008/009/010 llevan su qa-plan.md DENTRO del change de
  # backend). Acá la automatización vive en el change de FE —T5.1 e2e sobre el HTML servido,
  # T3.2 a11y con axe, T4.3 contrato de versión— y la trazabilidad AC→task está en la matriz
  # del tasks.md. Lo que queda de QA es la parte MANUAL del DoD, que no necesita un change.
  # 2026-08-23: el change de FE está IMPLEMENTED (12/12 tasks; 648 unit + 52 E2E verdes en
  # @dsm/web). La US NO pasa a Done y son dos motivos concretos, no burocracia:
  #   1. El checkbox de consentimiento y su captura son de US-008 (FE+BE) — el seam ya existe
  #      (`CONSENT_COPY` en src/features/legal/routes.ts), pero nadie lo consume todavía.
  #   2. El texto legal es PROVISIONAL con marcadores `[PENDIENTE: …]` visibles en la página.
  #      Publicar así es incumplimiento con apariencia de cumplimiento. El gate es humano
  #      (dueño + asesoría legal) porque el PO descartó el automático (OQ-FE-17 (a)).
  #
  # NOTA (2026-09-06, /archive-change US-017-paginas-legales-consentimiento-frontend-web):
  # el change de FE (24/24 tasks en su tasks.md actual) estaba mergeado a main desde
  # 2026-08-29 (bundle "Entrega 2", PR #3) pero openspec-changes.yaml nunca se había
  # actualizado tras ese merge — drift de índice puro, corregido ahora (status → archived).
  # Re-verificado el estado de los dos motivos que dejó abierto la nota de 2026-08-23:
  #   1. RESUELTO — `ConsentCheckbox.tsx` (US-008 FE, PR #23, mergeado) SÍ consume
  #      `CONSENT_COPY`/`LEGAL_ROUTES` de `features/legal/routes.ts` (verificado leyendo
  #      el componente real, no asumido).
  #   2. SIGUE ABIERTO — `apps/web/src/features/legal/content.ts` todavía tiene los 3
  #      marcadores `[PENDIENTE: …]` (razón social/CUIT/domicilio, plazo de retención,
  #      condición IVA) verificados en el código actual, sin cambios desde el diseño
  #      original. Gate humano (dueño + asesoría legal), no técnico — ninguna sesión de
  #      Claude puede cerrarlo.
  # `status` de la US se queda en `In Progress` por el motivo 2, ya con FE archivado —
  # única disciplina con change (QA vive dentro del código de FE, sin change propio, per
  # la norma del repo documentada arriba).
- id: US-018
  slug: contacto-whatsapp
  status: Done
  priority: Medium
  prd-capacity: 12
  disciplines: [FE]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-06-15
  ready-at: 2026-06-15
  in-progress-at: 2026-08-19        # first plan invocation (frontend-web-developer)
  done-at: 2026-08-29               # US-018-contacto-whatsapp-frontend-web archived; único discipline change de esta US
  blocked_by: [US-001]
- id: US-019
  slug: provision-plataforma-cloud
  status: In Progress
  priority: Medium
  disciplines: [INFRA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-08-09
  ready-at: 2026-08-09
  in-progress-at: 2026-08-16
  blocked_by: []          # gated en dependencias externas (cuentas/billing), no en otra US
- id: US-020
  slug: borrado-cuenta-datos-personales
  # /archive-change US-020-borrado-cuenta-datos-personales-frontend-web (2026-09-06, PR #96
  # mergeado 7c19fe2): FE archivada, extiende openspec/specs/retencion-datos-personales/
  # con la UI de /mi-cuenta. BE (PR #93) y FE (PR #96) ya archivadas — sólo QA (declarada
  # en disciplines) sigue activa, en desarrollo. status: QA per el criterio del skill
  # archive-change (disciplina QA pendiente → QA, no Done).
  # /archive-change US-020-borrado-cuenta-datos-personales-qa (2026-09-06, PR #98
  # mergeado ebfa7c0): QA archivada — última disciplina de la US. 26/26 escenarios de
  # aceptación + k6 (p95≈15.4ms vs 500ms) verdes. Sin delta de capability propio (el
  # enum account_deletion sobre ordenes/ ya lo aplicó el archive del backend). BE, FE y
  # QA resueltas → Done.
  status: Done
  priority: Medium
  prd-capacity: 13   # CAP-10 partida el 2026-08-23: la protección de datos es CAP-13
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-08-18
  ready-at: 2026-09-06
  t-shirt: M
  # NOTA (2026-09-06, /enrich-user-story US-020): promovida Backlog → Ready. DoR verde,
  # 15 AC en Gherkin (3 happy / 5 alternative / 7 negative-space), 8 SP tradicional /
  # 4 AI-asistido. Las 5 decisiones de producto quedaron cerradas por el PO y escritas
  # como `[Resolved]` en §10 — ninguna `[Open]`, ninguna `[Deferred]`:
  #   1. Anonimizar la fila de `customers` (soft-delete), no borrado físico.
  #   2. El email queda LIBERADO — se puede volver a registrar con el mismo correo.
  #   3. Inmediato e irreversible al confirmar (sin ventana de gracia ni job diferido).
  #   4. Autoservicio del cliente (contrasta con US-021, donde el dueño ejecuta el
  #      pedido del comprador invitado, que no tiene cuenta ni identidad verificable).
  #   5. Las órdenes EN CURSO (`pending_payment|new|preparing|ready`) BLOQUEAN el
  #      borrado; `delivered`/`cancelled` no. Verificación al ejecutar, no al mostrar.
  # Se corrigió una premisa falsa que el esqueleto propagaba ("borrar rompe la
  # integridad referencial"): las 4 relaciones de `customers` ya declaran su `onDelete`
  # (`orders`/`carts` SetNull, `refresh_tokens`/`password_reset_tokens` Cascade). La
  # razón real para anonimizar es conservar el vínculo orden↔cliente que sostiene las
  # métricas de US-016.
  # `blocked_by` ampliado de [US-014] a [US-014, US-015, US-021] — ver §6 de la US: las
  # tres tienen su backend mergeado a `main`, así que ninguna impide arrancar hoy, pero
  # son aristas reales del DAG (US-015 escribe `orders.customer_id`, sin el cual "las
  # órdenes de este cliente" no es un conjunto computable; US-021 aporta el mecanismo de
  # anonimización que esta US reusa en vez de duplicar).
  blocked_by: [US-014, US-015, US-021]
  in-progress-at: 2026-09-06   # /develop-backend US-020 — 27/27 tasks cerradas
  in-review-at: 2026-09-06     # BE PR #93 abierto (/commit) — primera disciplina en review
  done-at: 2026-09-06          # 3/3 disciplinas archivadas (/archive-change QA, PR #98)
  # /archive-change US-020-borrado-cuenta-datos-personales-backend (2026-09-06, PR #93
  # mergeado 0eea71b): BE archivado, extiende openspec/specs/retencion-datos-personales/
  # (CAP-13) con DELETE /me + delta cruzado sobre openspec/specs/ordenes/ (enum de
  # anonymization_reason). La US se queda en In Progress — FE y QA (también declaradas
  # en disciplines) todavía no se planificaron, así que no hay 3/3 disciplinas resueltas.
- id: US-021
  slug: retencion-datos-ordenes
  status: Done   # /archive-change US-021-retencion-datos-ordenes-frontend-web 2026-09-06 —
                 # 3/3 disciplinas archivadas: BE (#41/#48), QA (#51, inline en el backend),
                 # FE (#54). done-at abajo.
  priority: High
  prd-capacity: 13   # CAP-10 partida el 2026-08-23: la protección de datos es CAP-13
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-08-22
  ready-at: 2026-08-22
  in-progress-at: 2026-08-30        # primera planificación (/plan-backend-ticket)
  in-review-at: 2026-09-05          # FE PR #54 abierto (/commit) — primera vez que se
                                     # estampa este campo: BE y QA ya habían pasado por
                                     # review y se archivaron sin que quedara registrado acá
  done-at: 2026-09-06                # 3/3 disciplinas archivadas (/archive-change FE)
  # Origen: hueco detectado por el plan de backend de US-008 al modelar la PII del checkout
  # (su OQ-BE-5). El PO decidió el 2026-08-22 abrirla como CONDICIÓN PREVIA A PRODUCCIÓN.
  # US-020 cubre el borrado de CUENTAS; el comprador invitado no tiene cuenta que borrar, y
  # es el camino principal del PRD §2.1 cap. 4. Anonimiza, NO borra: el E2E §8 fija que las
  # órdenes no se borran (historial 12 meses + métricas de US-016).
  #
  # NOTA (2026-09-05, /commit US-021-retencion-datos-ordenes-backend, barrido de changes
  # pendientes de archivar): la disciplina backend estaba 30/30 tasks cerrada desde
  # 2026-08-23 pero nunca se había pusheado (sólo la planificación había mergeado, PR #25).
  # Publicado ahora — PR #41 abierto contra main. `status` de la US NO avanza: falta
  # merge + archive del backend, y hay un worktree/rama `feat/US-021-retencion-datos-ordenes-qa`
  # en curso (otra sesión) para la disciplina QA. `blocked_by` sin tocar.
  #
  # NOTA (2026-09-05, /archive-change US-021-retencion-datos-ordenes-backend, PR #41
  # mergeado): disciplina backend archivada (openspec/changes/archive/US-021-...
  # -backend/), 30/30 tasks. Crea la capacidad nueva
  # `openspec/specs/retencion-datos-personales/` (CAP-13) — README + requirements +
  # decisions + contratos vivos (2 endpoints admin, seedeados desde los yaml draft
  # del change porque nunca llegaron al spec publicado del servicio). `status` de la
  # US NO avanza: QA sigue en curso (otra sesión, worktree
  # `feat/US-021-retencion-datos-ordenes-qa`) y FE NO existe como change ni parece
  # absorbido por US-012 (a diferencia de US-023, cuyo FE sí quedó cubierto por
  # PendingPaymentsPanel — acá no hay referencia a anonimización en
  # apps/web/src/features/orders/). Falta confirmar con el PO/Arquitecto si la
  # acción "anonimizar" en el panel es un change FE propio o se agrega a US-012 FE.
  # `blocked_by` sin tocar.
  #
  # NOTA (2026-09-05, /develop-qa US-021, PR #51 mergeado): disciplina QA cerrada —
  # Mode A (inline en `openspec/changes/archive/US-021-...-backend/qa-plan.md`, no
  # hay change `-qa` separado que archivar). Suite de aceptación real contra el
  # backend mergeado: 11/11 escenarios (74/74 steps) + contract testing 11/11 casos,
  # verde, reproducido en corridas consecutivas. Sin defectos en la implementación.
  # Gaps documentados, no rellenados en falso: TC-021-004b bloqueado — el DTO de
  # US-012 (`AdminOrderSummaryDto`/`AdminOrderDetailDto`) no expone `anonymized_at`/
  # `anonymization_reason`, PR #41 no lo cerró; TC-021-007 manual (consentimiento,
  # sin superficie API); QA-021-CT-2 sin contract test de US-012 contra el que
  # comparar. El gap del DTO queda a cargo de quien planifique el FE de US-021
  # (`a7`, en curso) — extenderlo es la vía más chica para completar AC-4/AC-5 a
  # nivel UI. `status` de la US sigue sin avanzar: sólo falta FE. `blocked_by` sin
  # tocar.
  #
  # NOTA (2026-09-05, /commit US-021-retencion-datos-ordenes-frontend-web, PR #54
  # abierto): el gap del DTO que dejó QA (TC-021-004b) se resolvió antes de planificar
  # el FE, en un change de backend chico dedicado (PR #52, mergeado) — no tocó el
  # change de US-012 como sugería la nota de QA, fue un follow-up propio. FE: 27/27
  # tasks cerradas (acción "anonimizar" con confirmación de dos pasos, indicación de
  # anonimizado en `OrderDetail`, `retention-sweep` fuera de alcance del panel). Con
  # esto las 3 disciplinas tienen PR (BE #41/#48 archivado, QA #51 archivado inline,
  # FE #54 en review) — `status` avanza a `In Review`; pasa a `Done` cuando FE también
  # se mergee y archive.
  blocked_by: [US-008]    # tiene que existir la orden con PII antes de poder anonimizarla
- id: US-022
  slug: actualizacion-dependencias-frontend
  status: Done   # /archive-change 2026-09-06: FE (PR #95) y QA (PR #102) mergeados y
  # archivados — las 2 disciplinas declaradas resueltas. Sin capacidades/contratos
  # nuevos (bump de dependencias + revalidación, sin superficie visible).
  priority: High
  prd-capacity: null            # no nace del PRD: hallazgo del gate de seguridad de US-006
  disciplines: [FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-08-23
  ready-at: 2026-09-06
  in-progress-at: 2026-09-06    # primera planificación (frontend-web-developer, /plan-frontend-web-ticket)
  in-review-at: 2026-09-06      # FE (PR #95) + QA (PR #102)
  done-at: 2026-09-06
  blocked_by: []
- id: US-023
  slug: pago-manual-offline
  status: Done   # /archive-change 2026-09-05 — 3/3 disciplinas resueltas: BE archivado (PR #27/#40),
                 # FE satisfecho por US-012-pendientes-pago-frontend-web (PR #31, archivado),
                 # QA archivado (PR #38). Ver notas de abajo por el detalle de cada una.
  priority: High
  prd-capacity: 4   # CAP-4 «Checkout y pago» — adaptador de pago sin MercadoPago
  disciplines: [BE, FE, QA]
  parent-prd: docs/product/prd.md
  linear-issue-id: null
  created: 2026-08-30
  ready-at: 2026-08-30
  in-progress-at: 2026-08-30   # /plan-backend-ticket — primer plan de disciplina abierto
  in-review-at: 2026-08-30     # backend PR #27 (mergeado 2026-09-02, archivado 2026-09-05) + QA
                                # PR #38 (mergeado y archivado 2026-09-05, ver nota final) — FE
                                # quedó satisfecho el 2026-09-05 vía PR #31 (ver nota abajo)
  done-at: 2026-09-05
  t-shirt: M
  # Pago manual/offline (transferencia/efectivo, coordinado por WhatsApp): el dueño confirma
  # el pago y la orden pasa a `new` (FSM E2E §12, no "paid" — corregido en el enrich).
  # US-023 es DUEÑA del PaymentConfirmationPort (extraído de US-009); MercadoPago (US-009)
  # queda como otro adaptador del mismo puerto, `Blocked` por dependencia externa
  # (credenciales) — confirmado 2026-08-30 por la sesión que planificó US-012 (ver su
  # frontmatter + docs/user-stories/US-009-pago-mercadopago.md §10).
  # AC-2 fija un requisito nuevo para US-012 (vista separada de pending_payment en el panel);
  # US-012-panel-ordenes-dueno-backend (PR #22) se está re-planificando 2026-08-30 para
  # depender de PaymentConfirmationPort/payments de esta US en vez de la cadena US-009→US-010.
  # 2026-08-31: /plan-frontend-web-ticket US-023 NO abrió change — la Fase 12 de
  # US-012-panel-ordenes-dueno-frontend-web (PR #22, `PendingPaymentsPanel.tsx`) ya construye
  # el FE completo de esta US (confirmar pago + vista de pendientes), consumiendo sus dos
  # endpoints. FE-US-023 queda sin change propio; no es una disciplina huérfana, está
  # satisfecha por el change de US-012.
  # 2026-09-05 (hallazgo de /develop-qa US-023, verificado leyendo código — no asumido, EN
  # SU MOMENTO): la afirmación de la nota de 2026-08-31 ("Fase 12 ya construye el FE
  # completo") era INCORRECTA en ese punto — `find`/`git log --all` no encontraban
  # `PendingPaymentsPanel` en ningún lado, y T12.1-T12.4 seguían `- [ ]` en
  # `US-012-panel-ordenes-dueno-frontend-web/tasks.md`. Efecto en ese momento: la mitad UI
  # de AC-2 y `QA-023-E2E-1` quedaron genuinamente bloqueados en el qa-plan.md de esta US
  # (manual/blocked, no inventado) — hallazgo correcto para su fecha, ya no vigente (ver
  # nota siguiente).
  #
  # NOTA (2026-09-05, /archive-change US-023-pago-manual-offline-backend, barrido de
  # archivos pendientes — PR #27 mergeado 2026-09-02): disciplina backend archivada
  # (openspec/changes/archive/US-023-pago-manual-offline-backend/), 28/28 tasks. Crea la
  # capacidad nueva `openspec/specs/pagos/` (CAP-4, camino manual/offline) — README +
  # requirements + decisions + contratos vivos (POST .../confirm-payment,
  # GET .../pending-payment).
  #
  # RESUELTO más tarde el mismo 2026-09-05: `PendingPaymentsPanel.tsx` +
  # `pendingPaymentsService.ts` se construyeron en un change dedicado
  # (`US-012-pendientes-pago-frontend-web`, PR #31, archivado 2026-09-05) — verificado que
  # ambos archivos existen en el árbol real post-merge. FE-US-023 SÍ está satisfecho ahora.
  #
  # NOTA FINAL (2026-09-05, /archive-change US-023-pago-manual-offline-qa, PR #38 mergeado):
  # las 3 disciplinas quedan resueltas — BE archivado (PR #27/#40), FE satisfecho por
  # US-012-pendientes-pago-frontend-web (PR #31, archivado), QA archivado (PR #38, 4/4
  # stubs automatizados verdes). `status` de la US avanza a `Done`. Pendiente (no
  # bloqueante, follow-up sugerido): `QA-023-E2E-1` en el qa-plan.md quedó scaffoldeado
  # como manual/blocked antes de que PR #31 aterrizara — ahora que el componente existe,
  # alguien puede des-diferirlo y automatizarlo.
  blocked_by: [US-008, US-012]    # US-008: orden en `pending_payment` (satisfecho). US-012: el panel donde vive la acción — Fase 12 (FE) construida vía PR #31, cubre AC-2 de esta US (satisfecho).
